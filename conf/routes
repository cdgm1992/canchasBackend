# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Controlador de canchas
GET     /canchas               infraestructura.cancha.controlador.ConsultaCanchaControlador.listar()
GET     /canchas/:id           infraestructura.cancha.controlador.ConsultaCanchaControlador.buscarCancha(id: Long)
POST    /canchas               infraestructura.cancha.controlador.ComandoCanchaControlador.crear()
DELETE  /canchas/:id           infraestructura.cancha.controlador.ComandoCanchaControlador.eliminar(id: Long)